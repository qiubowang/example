<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ThemeBase" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="MaterialProgressBarHorizontal">@style/material_progressbar_horizontal</item>
    </style>
    <style name="material_progressbar_horizontal">
        <item name="indeterminate">true</item>
        <item name="barColor">#ff0ea7fa</item>
        <item name="bgColor">#ffb3e1fa</item>
        <item name="duration">1200</item>
    </style>

</resources>
